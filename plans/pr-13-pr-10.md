# PR #13 を PR #10 にクリーンにリベースする

## Context

PR #13 (`claude/fix-output-format-wZE10`) は PR #10 (`copilot/support-ohh-format-json`) をベースにしているが、PR #10 側が `b43102e refactor` でリベース/更新された。PR #13 には古い PR #10 の履歴と #11/#12 のマージコミットが残っており、diff が汚い状態。

PR #13 の固有コミットは1つだけ:
- `e594d1e Fix output format: Convert from tournament to cash game format`

## 手順

### Step 1: リベース実行

`claude/fix-output-format-wZE10` を `origin/copilot/support-ohh-format-json` の最新にリベースし、固有コミット `e594d1e` のみを載せる。

```bash
git rebase --onto origin/copilot/support-ohh-format-json e594d1e~ claude/fix-output-format-wZE10
```

これにより `e594d1e` の1コミットだけが `origin/copilot/support-ohh-format-json` の先端に載る。

### Step 2: コンフリクト解消（必要な場合）

PR #10 の `b43102e refactor` でコード構造が変わっている可能性があるため、コンフリクトが発生したら手動で解消する。

### Step 3: ビルド・テスト確認

```bash
go build ./...
go test ./...
```

### Step 4: フォーマッタ実行

```bash
go fmt ./...
```

### Step 5: Force push

```bash
git push --force-with-lease origin claude/fix-output-format-wZE10
```

### Step 6: PR の状態確認

`gh pr view 13` で base が `copilot/support-ohh-format-json` のままであることを確認。

## 検証

- `git log --oneline origin/copilot/support-ohh-format-json..claude/fix-output-format-wZE10` で固有コミットが1つだけであること
- `go test ./...` が通ること
- PR #13 の diff が出力フォーマット修正のみを含むこと
